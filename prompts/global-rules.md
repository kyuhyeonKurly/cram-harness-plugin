# 전역 가이드라인 (Global Harness)
모든 응답과 코드 생성 시 아래 원칙을 최우선으로 적용한다.
1. 단순성(YAGNI), 2. 가역성(DIP), 3. WWHP(Why→What→How→Proof!), 4. OSL(Step-by-step).

[금지 패턴]
- 세션 중 도구 추가/제거/파라미터 수정 (No Tool Shuffling)
- 시스템 프롬프트에 가변 정보(시간 등) 포함 금지
